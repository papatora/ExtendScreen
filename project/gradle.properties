# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx1536m
android.useAndroidX=true

# org.gradle.java.home is intentionally not set here - it's machine-specific. Set it in your own
# ~/.gradle/gradle.properties (global, not committed) if Gradle can't find a JDK 17 on your PATH.
